DATABASE_URL="postgresql://user:pass@host/db?pgbouncer=true&connection_limit=5&pool_timeout=20"
DIRECT_URL="postgresql://user:pass@host/db"
AUTH_SECRET=""           # openssl rand -base64 32
AUTH_URL="http://localhost:3000"
OTP_PEPPER=""            # random 32+ char string
SMS_PROVIDER="console"   # "console" dev | "msg91" prod
MSG91_AUTH_KEY=""
MSG91_TEMPLATE_ID=""

# Email (Nodemailer) — leave blank to use console stub in dev
SMTP_HOST=""
SMTP_PORT=465
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""             # e.g. "Wolsell <no-reply@wolsell.com>"
CONTACT_EMAIL="admin@wolsell.com"

# Site URL (for SEO sitemap + robots)
NEXT_PUBLIC_SITE_URL="https://wolsell.com"
