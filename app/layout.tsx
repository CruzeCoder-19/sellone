import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import { Providers } from "./providers";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL || "https://wolsell.com"),
  title: {
    default: "Wolsell — Wholesale Supplies, Simplified",
    template: "%s — Wolsell",
  },
  description:
    "India's trusted B2B wholesale marketplace. Bulk pricing on hardware, sanitary, electrical, paints, apparel, and more. Register as a buyer or sell your products.",
  openGraph: {
    type: "website",
    siteName: "Wolsell",
    locale: "en_IN",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
